import 'package:flutter/material.dart';

class BuffScreen extends StatelessWidget {
  const BuffScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
